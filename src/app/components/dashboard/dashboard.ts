'use strict';

class Dashboard {

  // static $inject: string[] = [
  // ];

  // constructor(
  //     ) {
  // }

  //     $onActivate (toRoute, fromRoute) {
  // 	// this.name = toRoute.params.name;
  // }
}

export let dashboard = {
  templateUrl: 'app/components/dashboard/dashboard.html',
  controller: Dashboard
};